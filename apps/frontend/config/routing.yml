# custom rules
homeGuest:
  url:   /homeGuest/index
  param: { module: homeGuest , action: index }

action_to_template:
  url:   /homeGuest/actiontotem
  param: { module: homeGuest , action: actiontotem }

  
  
# default rules
homepage:
  url:   /
  param: { module: homeGuest, action: home }

default_index:
  url:   /:module
  param: { action: index }

default:
  url:   /:module/:action/*

parameter_default_index:
  url:   /:module/*
  param: { action: index }
  
